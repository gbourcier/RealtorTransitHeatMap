basePath: /
definitions:
  httpapi.FetchPricesResponse:
    properties:
      listings:
        items:
          $ref: '#/definitions/httpapi.ListingDTO'
        type: array
    type: object
  httpapi.ListingDTO:
    properties:
      address:
        type: string
      latitude:
        type: number
      longitude:
        type: number
      mls:
        type: string
      price:
        type: number
    type: object
host: localhost:3000
info:
  contact: {}
  description: API for fetching real estate listings using the operator-configured
    search parameters.
  title: Realtor Transit Heat Map API
  version: "1.0"
paths:
  /fetchPrices:
    get:
      description: Fetch real estate listings using the operator-configured search
        parameters
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/httpapi.FetchPricesResponse'
        "408":
          description: request cancelled
          schema:
            type: string
        "500":
          description: internal server error
          schema:
            type: string
      summary: Fetch listings
      tags:
      - listings
swagger: "2.0"
