# Copy this file to .env and adjust as needed.
# .env is gitignored.

POSTGRES_DB=realtor_heatmap
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# Used by sqlx-cli and the Rust app to connect to the running container.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/realtor_heatmap
