# Go build artifacts
/bin/
*.test
*.out

# Editor: Zed
#  .zed/tasks.json is checked in (shared run tasks).
#  User-local Zed state lives in ~/.config/zed and is not in this repo.

# Local environment files (DB credentials, etc.)
.env
